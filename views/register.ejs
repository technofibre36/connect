<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>InvestConnect — Register</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body{margin:0;font-family:Inter,Arial,Helvetica,sans-serif;background:linear-gradient(180deg,#071028 0%,#071733 60%);color:#e6eef8;display:flex;align-items:center;justify-content:center;height:100vh}
        .card{width:460px;background:rgba(255,255,255,0.02);padding:28px;border-radius:12px;border:1px solid rgba(255,255,255,0.03)}
        h2{margin:0 0 6px}
        .muted{color:#9aa6bd}
        input{width:100%;padding:12px;margin-top:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:inherit}
        .btn{margin-top:14px;width:100%;padding:12px;border-radius:10px;border:none;background:#12b886;color:#042028;font-weight:700;cursor:pointer}
        .small{font-size:13px;color:#9aa6bd;margin-top:10px}
    </style>
</head>
<body>
    <div class="card">
        <h2>Create your account</h2>
        <div class="muted">Join InvestConnect — share ideas and follow portfolios</div>

        <% if (typeof error !== 'undefined' && error) { %>
            <p style="color:#ffb3b3"><%= error %></p>
        <% } %>

        <form action="/register" method="POST">
            <input type="text" name="name" placeholder="Full Name" required value="<%= typeof name !== 'undefined' ? name : '' %>" />
            <input type="email" name="email" placeholder="Email" required value="<%= typeof email !== 'undefined' ? email : '' %>" />
            <input type="password" name="password" placeholder="Password" required />
            <button class="btn" type="submit">Create Account</button>
        </form>

        <div class="small">By creating an account you agree to our <a style="color:#9aa6bd" href="#">Terms</a>.</div>
        <div class="small">Already have an account? <a style="color:#9aa6bd" href="/login">Sign in</a></div>
    </div>
</body>
</html>
